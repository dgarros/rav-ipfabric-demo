---
- name: Deploy Application on Ravello
  connection: local
  hosts: all
  gather_facts: no
  roles:
    - ravello.lib
    - ravello.deploy_topology
  pre_tasks:
    - include_vars: user-input.yaml
